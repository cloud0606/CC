diff --git a/docker/html/src/object/order.php b/docker/html/src/object/order.php
index bb80917..9624bd9 100644
--- a/docker/html/src/object/order.php
+++ b/docker/html/src/object/order.php
@@ -20,9 +20,11 @@ class Order{
     function getOrderInfo($orderid,$userid) {
         try {
              $sql = "select orderid, userid, prodid, totalPrice,createtime from ".$this->table_name.
-            " where orderid=".$orderid." and userid='".$userid."'";
+            " where orderid=:orderid and userid=:userid ";
 	    $stmt = $this->conn->prepare($sql);
-            $stmt->execute();
+            $stmt->bindParam ( ':orderid', $orderid );	    
+	    $stmt->bindParam ( ':userid', $userid );
+	    $stmt->execute();
             $result =$stmt->fetch(PDO::FETCH_LAZY); // 查询单条数据
             return $result;
         } catch(PDOException $e) {
@@ -35,8 +37,9 @@ class Order{
         try {
             $sql = "insert into ".$this->table_name.
             " (userid, prodid,totalPrice) values (".
-            $userid.", ".$prodid.", ".$totalPrice.")";
-	        $stmt = $this->conn->prepare($sql);
+            $userid.", :prodid, ".$totalPrice.")";
+	    $stmt = $this->conn->prepare($sql);
+	    $stmt->bindParam ( ':prodid', $prodid );
             $stmt->execute();
             return $this->conn->lastInsertId(); // 返回订单ID
         } catch(PDOException $e) {
